import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const LinkedListVisual = () => {
    return (
        <View>
            <Text>Linked List Visual</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default LinkedListVisual;
